# ORRERY pager — Pi Zero 2 W. Install into a venv on the device:
#   python3 -m venv ~/pager-venv && ~/pager-venv/bin/pip install -r requirements.txt
requests>=2.31
Pillow>=10.0
numpy>=1.24
# Hardware-only (comes from the vendored PiSugar driver, NOT pip):
#   whisplay  → clone github.com/PiSugar/whisplay, put runtime/ on PYTHONPATH
#   RPi.GPIO / gpiod / spidev → per the Whisplay install docs
