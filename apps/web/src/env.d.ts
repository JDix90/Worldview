/// <reference types="vite/client" />

/** Injected by vite.config.ts `define` from the root .env (ORRERY_AUTH_TOKEN). */
declare const __ORRERY_TOKEN__: string;
/** Injected by vite.config.ts `define` from the root .env (FIRMS_MAP_KEY); empty when unset. */
declare const __FIRMS_KEY__: string;
